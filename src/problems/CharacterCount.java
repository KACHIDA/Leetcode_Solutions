package problems;

public class CharacterCount {

    public static void main(String[] args)
    {
        String[] A= {"a","bc","def","abc","f"};
        System.out.println(character_count(A));
    }

    public static int character_count(String[] str) {
        if (str.length == 0) {
            return 0;
        } else{
            //return str[0].length() + character_count();

            return 0;
    }
    }
}
